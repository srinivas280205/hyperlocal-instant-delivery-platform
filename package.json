{
  "name": "quickdrop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cp -r build ../build",
    "vercel-build": "cd frontend && npm install --legacy-peer-deps && npm run build && cp -r build ../build"
  }
}
